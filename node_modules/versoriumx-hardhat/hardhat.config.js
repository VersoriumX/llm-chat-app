require("@nomicfoundation/hardhat-toolbox");

/** @type import('hardhat/config').HardhatUserConfig */

module.exports = {
  solidity: "0.8.28",
  networks: {
    ethereum: {
      url: process.env.ETHEREUM_URL || "https://eth.llamarpc.com", // Example URL, replace with your actual Ethereum RPC URL
      forking: {
        url: process.env.ETHEREUM_FORK_URL || "https://eth.llamarpc.com"
      }
    },
    optimism: {
      url: process.env.OPTIMISM_URL || "https://optimism.llamarpc.com", // Example URL, replace with your actual Optimism RPC URL
      forking: {
        url: process.env.OPTIMISM_FORK_URL || "https://optimism.llamarpc.com", //Optimism mainnet forking
      }
    },
    polygon: {
      url: process.env.POLYGON_URL || "https://polygon.llamarpc.com", // Example URL, replace with your actual Polygon RPC URL
      forking: {
        url: process.env.POLYGON_FORK_URL || "https://polygon.llamarpc.com", //Polygon mainnet forking
      }
    },
  },
};
