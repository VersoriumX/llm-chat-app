# backend/response/return_to_sender.py

import smtplib
from email.mime.text import MIMEText

class ReturnToSender:
    def __init__(self, smtp_server, smtp_port, username, password):
        self.smtp_server = smtp_server
        self.smtp_port = smtp_port
        self.username = username
        self.password = password

    def send_alert(self, sender_email):
        msg = MIMEText("Your email has been flagged as potentially malicious. Please verify your intentions.")
        msg['Subject'] = "Alert: Potential Threat Detected"
        msg['From'] = self.username
        msg['To'] = sender_email

        with smtplib.SMTP(self.smtp_server, self.smtp_port) as server:
            server.login(self.username, self.password)
            server.send_message(msg)
